class PluginRepoHelper {
  repo_url: string;

  async getPlugins() {
    /**
     * TODO: get plugins from repo and save the json file to local storage
     */
  }

  async checkIfValid() {
    /**
     * TODO: check if repo json file is valid
     */
  }

  async checkIfUpToDate() {
    /**
     * TODO: check if repo json file is up to date
     */
  }
}

export default PluginRepoHelper;
